pms_qc_ext_overlay_application:
  schema_version: "PMS-QC_EXT_1.1"
  schema_role: "case_extension_overlay_application"
  overlay_status: "optional_paper_specific_refinement_layer"
  overlay_source_file: "PMS-QC-EXT.yaml"
  base_qc_overlay_source_file: "PMS-QC.yaml"
  base_reference_file: "PMS.yaml"
  base_schema_version: "PMS_1.3"
  extends:
    base_model: "PMS-QC_1.1"
    base_dependency: "PMS_1.3"
    compatibility: >
      Declared for PMS-QC_1.1 as an optional paper-specific refinement layer.
      This extension does not override PMS or PMS-QC base semantics.

  pms_application_reference:
    note: >
      Any use of PMS-QC_EXT that claims PMS-based application remains
      subordinate to the PMS_1.3 entry condition and self-binding clause. This
      extension does not suspend, replace, or override PMS application
      guardrails.
    refers_to:
      - "pms_entry_condition"
      - "schema_meta.normative_position.self_binding_clause"
      - "repository_governance"
    case_application: >
      PMS application guardrails remain active because this EXT pass is only an
      optional refinement overlay appended to the completed PMS Core case and the
      PMS-QC base overlay. Distance (Χ), reversibility, Dignity-in-Practice as a
      praxeological application constraint, non-capture, rival openness, and
      non-self-validation remain controlling boundaries.

  extension_meta:
    model_name: "Praxeological Meta-Structure – Quantum Computing Extension Layer"
    model_short: "PMS-QC_EXT"
    status: "draft"
    repository_role: "paper_specific_qc_refinement_layer"
    layer_role: "optional_extension"
    optional_extension: true
    depends_on:
      - "PMS_1.3"
      - "PMS-QC_1.1"
    epistemic_status: >
      Optional paper-derived refinement layer for bounded structural analysis,
      annotation, and policy experimentation within the PMS-QC framework.
    boundedness_note: >
      PMS-QC_EXT is not a generic QC standard and not a canonical part of PMS.
      It collects paper-specific refinements that remain subordinate to PMS and
      the PMS-QC base overlay. Use of EXT does not imply comparative
      superiority, exhaustive capture, deployment adequacy, or validated safety.
    description: >
      PMS-QC_EXT adds paper-specific macro refinements, Δ–Ψ chain templates,
      attractor schedule patterns, ladder refinements, measurement-domain
      modelling refinements, and optional structural policy refinements derived
      from the present PMS-QC paper. It extends the expressive range available
      for paper-aligned analysis without altering the semantics of the generic
      PMS-QC base overlay. Nothing in this file should be read as introducing
      new PMS meta-axioms, a new operator layer, or a uniquely privileged
      architecture for AI–QC governance. In a generic workflow-governance case,
      EXT should normally function as a boundary and applicability check before
      it functions as an active algorithmic refinement layer.
    authors:
      - name: "T. Zöller"
        role: "originator"
      - name: "ChatGPT (GPT-5.4 Thinking)"
        role: "extension_formalization"
    case_application: >
      PMS-QC_EXT is used only because the case title and prior QC overlay focus
      on iteration depth, repetition limits, audit trace, and workflow
      governance, which are precisely the areas where the extension can test
      whether paper-specific refinements such as ladder depth guards,
      attractor-cycle integrity, measurement-domain modelling, and post-Σ
      reframing guards are warranted. The result is mostly negative or weak:
      the case does not supply QPE, Grover, explicit attractor schedules, or a
      physical measurement model, so EXT remains a bounded optional audit layer
      rather than a substantive algorithm-specific refinement pass. Its primary
      value here is therefore discriminative: it marks which paper-specific QC
      refinements are not warranted, while preserving a small number of weak
      audit-policy checks where the case material actually touches EXT-relevant
      structure.

  source_header_alignment:
    intended_use:
      - "Attach alongside PMS.yaml and PMS-QC.yaml for paper-specific QC analysis, annotation, and bounded refinement work."
      - "Support optional reasoning about QPE ladders, fixed-point Grover variants, attractor schedules, and bounded governance-oriented experimentation."
      - "Provide paper-derived refinement templates for documentation, orchestration, and compilation-adjacent analysis."
    not_intended_for:
      - "redefining PMS or PMS-QC base semantics"
      - "establishing a generic QC standard"
      - "validated safety architecture"
      - "deployment-ready agent control"
      - "physical correctness guarantees"
      - "architecture-independent implementation claims"
      - "anthropomorphic interpretation of quantum processes"
    repositories:
      PMS_base: "https://github.com/tz-dev/Praxeological-Meta-Structure-Theory"
      PMS_QC: "https://github.com/tz-dev/PMS-QC"
    relationship_to_pms_qc:
      - "Supplements PMS-QC with paper-specific macro refinements, chain templates, and optional structural constraints."
      - "Never overrides base definitions."
      - "Never promotes paper-specific constructs into the generic base layer."
    case_application: >
      The source-header discipline applies by allowing only bounded annotation,
      paper-specific audit refinement, and optional policy experimentation. The
      case does not use EXT to create a generic QC standard, validate safety,
      certify deployment, replace PMS-QC base semantics, or anthropomorphise
      quantum workflow structure.

  load_order:
    required_order:
      - "PMS.yaml"
      - "PMS-QC.yaml"
      - "PMS-QC-EXT.yaml"
    rule: >
      PMS-QC_EXT may be applied only after PMS Base and PMS-QC base overlay have
      been read and preserved. EXT is optional and paper-specific. It cannot
      substitute for the PMS Core pass or for the generic PMS-QC overlay.
    case_application: >
      The required load order is satisfied for this application: PMS.yaml
      remains the only PMS Base reference, the completed PMS-QC base overlay is
      treated as already present, and PMS-QC-EXT.yaml is applied only as an
      optional extension profile subordinate to both.

  overlay_relationship_to_core_and_qc:
    status: "subordinate_to_completed_pms_core_and_qc_overlay"
    core_case_status: "already_completed"
    qc_base_overlay_status: "already_completed"
    core_case_source: "completed PMS Core case YAML for QC-Workflow Governance (Iteration depth) (Audit Trace)"
    qc_base_overlay_source: "completed PMS-QC overlay YAML for QC-Workflow Governance (Iteration depth) (Audit Trace)"
    rule: >
      This QC-EXT block is appended to or integrated with an already completed
      PMS Core v1.3 case artefact and an already completed PMS-QC base overlay.
      It does not rewrite the Core pass, redefine PMS operators, alter PMS
      dependencies, change derived structures, override PMS-QC base semantics,
      or silently promote paper-specific EXT refinements into the generic QC
      layer.
    preserved_core_elements:
      - "PMS operator inventory Δ, ∇, □, Λ, Α, Ω, Θ, Φ, Χ, Σ, Ψ"
      - "PMS operator order and dependency hygiene"
      - "PMS derived structures as derived, not primitive"
      - "PMS application guardrails"
      - "PMS non-diagnostic and non-person-ranking discipline"
      - "PMS non-capture and rival-openness discipline"
    preserved_qc_base_elements:
      - "PMS-QC generic base macros"
      - "PMS-QC base structural policies"
      - "PMS-QC non-guarantee clauses"
      - "PMS-QC base / extension boundary"
      - "PMS-QC structural-not-physical discipline"
      - "PMS-QC rival-formalism openness"
    integration_rule: >
      The QC-EXT overlay may add paper-specific refinements for QPE ladders,
      fixed-point or overshoot-regulated Grover variants, attractor schedules,
      measurement-domain modelling, and optional AI–QC policy heuristics. It
      must not modify the completed PMS Core case or PMS-QC base overlay except
      where a direct consistency error has been separately identified.
    case_application: >
      The EXT reading depends on the completed Core pass for Δ–Ψ structure and
      on the completed PMS-QC overlay for generic QC macros and base policies.
      In this case, EXT mostly confirms that algorithm-specific refinements are
      not warranted by the supplied material while allowing weak measurement-
      domain and policy-refinement checks around audit trace, iteration depth,
      and post-Σ reframing.

  normative_position:
    non_identity_clause: >
      EXT mappings and refinements are structural renderings only. They do not
      imply ontological identity between PMS operators and the specific
      algorithmic or governance constructs discussed in the paper.
    non_monopoly_clause: >
      PMS-QC_EXT does not claim monopoly over high-level structural analysis,
      nor does it imply that rival or complementary formalisms could not capture
      certain distinctions more effectively.
    anthropomorphism_clause: >
      Terms such as awareness, attractor, governance, or self-binding remain
      structural and non-anthropomorphic. EXT constructs do not attribute
      consciousness, intention, agency, or moral status to quantum systems.
    non_guarantee_clause: >
      Structural refinement does not imply physical correctness, convergence,
      stability, safety, or deployment readiness.
    governance_conditionality_clause: >
      Governance-oriented content in this extension is optional and conditional.
      It should be read as one bounded architectural path within the paper, not
      as the natural telos of PMS-QC or as a validated control architecture.
    base_vs_extension_clause: >
      Generic PMS-QC macros and policies remain in the base overlay.
      Paper-specific ladder structures, fixed-point Grover refinements,
      stronger attractor schedules, and additional policy heuristics remain
      EXT-only.
    case_application: >
      These clauses constrain the case by keeping QPE, Grover, attractor-schedule,
      and measurement-domain refinements optional, structural, and non-canonical.
      The case is not turned into a QPE case, a Grover case, a physical
      measurement theory, or an implementation-governance architecture merely
      because EXT vocabulary can annotate some audit and iteration risks.

  computational_position:
    representation_clause: >
      This file is a formal representational addendum for optional
      paper-specific refinements. It is not executable code, not a proof of
      implementability, and not a guarantee of interoperability.
    calibration_clause: >
      Structural well-formedness in PMS-QC_EXT does not by itself establish
      physical adequacy, numerical correctness, stability, safety, or optimal
      compilation.
    accountability_clause: >
      Any operational use of EXT remains dependent on external architectures,
      implementations, validation procedures, and domain-specific accountability
      mechanisms.
    deployment_clause: >
      EXT compliance or EXT policy satisfaction must not be treated as evidence
      of deployment readiness, safe orchestration, or validated agent control.
    case_application: >
      The case remains representational, annotation-oriented, and policy-
      experimental. It risks overclaim only if weak EXT policy checks are read
      as implementation sufficiency, validated audit architecture, runtime
      orchestration, convergence control, or deployment readiness. This block
      explicitly blocks those readings.

  semantic_anchor:
    note: >
      The workflow model below is one optional organizational abstraction for
      toolchains and analysis. It is non-canonical and does not redefine PMS or
      PMS-QC composition logic.
    qc_workflow_model: >
      A QC workflow may be modeled as a typed directed acyclic graph (DAG),
      where nodes represent macros, gates, measurements, or policy checks,
      edges represent causal, temporal, or data dependencies, and scopes are
      determined by active □-frames. This is a toolchain-oriented aid, not a
      replacement for canonical PMS composition rules.
    case_application: >
      DAG-style workflow modelling is relevant only as a possible audit-trace
      organization aid: iteration nodes, audit-record nodes, review nodes,
      policy-check nodes, and frame scopes could be represented as a typed DAG.
      This remains non-canonical and does not replace PMS composition logic,
      quantum circuit semantics, or implementation-specific workflow models.

  extension_governance:
    role_of_this_file: >
      Optional paper-specific refinement layer within the PMS-QC repository
      ecosystem.
    extensions_may:
      - "Define paper-specific refinements of generic PMS-QC macros."
      - "Introduce paper-specific chain templates for analysis and annotation."
      - "Add optional structural policy refinements and bounded governance heuristics."
      - "Add organizational or toolchain-oriented helper metadata when explicitly marked as non-canonical."
    extensions_should_not_without_explicit_revision:
      - "Silently redefine PMS or PMS-QC base semantics."
      - "Silently promote EXT refinements into the generic base layer."
      - "Present paper-specific constructs as generic PMS-QC standards."
      - "Treat governance-oriented refinements as validated deployment architecture."
      - "Treat helper metadata as if it modified canonical PMS operator logic."
    case_application: >
      Extension-governance discipline is preserved because the case uses EXT
      only to check whether paper-specific refinements are applicable. Most
      refinements remain weak or not applicable; none is promoted into PMS Base,
      PMS-QC base, or the completed Core case.

  base_vs_extension_boundary:
    note: >
      This block records the intended separation between the generic PMS-QC base
      overlay and the paper-specific EXT layer.
    refines_base_macros:
      - "QORACLE_CALL"
      - "QDIFFUSION"
      - "QMEASURE"
    reserved_ext_macros:
      - "QPE_ORACLE_LADDER"
      - "GROVER_CENTERING"
      - "ATTRACTOR_FIXED_POINT"
      - "MEASUREMENT_PROJECTION_MODEL"
    paper_specific_chain_patterns:
      - "qpe_exact"
      - "grover_fixed_point"
      - "attractor_alignment"
    paper_specific_policy_refinements:
      - "qpe_depth_guard"
      - "grover_no_overshoot"
      - "basis_reframe_guard"
      - "attractor_cycle_integrity"
    boundary_status: "preserved"
    case_application: >
      The case uses the PMS-QC base overlay as its main QC layer and invokes
      PMS-QC_EXT only for weak or risk-level checks. MEASUREMENT_PROJECTION_MODEL
      is weakly relevant to audit-trace interpretation, basis_reframe_guard is
      active as a post-Σ audit guard, and attractor_cycle_integrity is risk-level
      because iteration and recurrence are central. QPE_ORACLE_LADDER,
      GROVER_CENTERING, qpe_exact, grover_fixed_point, and attractor_alignment
      are not established. No EXT construct is promoted into the base layer.

  application_boundary:
    note: >
      EXT use remains bounded by the same non-anthropomorphic and
      non-deployment discipline that governs PMS and PMS-QC.
    constraints:
      - "No clinical, personal or moral interpretation of QC actors or workflows."
      - "No anthropomorphisation of quantum patterns."
      - "No inference from structural refinement to safety or capability."
      - "No deployment claim from EXT compliance alone."
    case_application: >
      The application boundary is preserved by treating all QC workflow terms as
      structural and non-anthropomorphic, making no person-ranking, clinical,
      legal, forensic, empirical, physical-correctness, safety, or deployment
      claim, and keeping EXT compliance separate from real-world capability.

  extended_qc_mappings:
    overview: >
      These are paper-specific refined renderings of selected Δ–Ψ operators in
      QC contexts. They do not override the generic PMS-QC base mappings and
      should be read as optional, non-canonical refinements used for the present
      paper's algorithmic and governance-oriented analysis.
    mapping_status: "partial"
    items:
      Delta:
        operator: "Δ"
        applicability: "weak"
        refined_roles:
          - "Phase-distinguished alternatives in eigenvalue encodings."
          - "Domain-partitioned initialisation in multi-register workflows."
        case_application: >
          Δ is not refined through a supplied phase-eigenvalue encoding or a
          concrete multi-register initialization. It remains weakly relevant
          only because audit and measurement-adjacent trace interpretation would
          require outcome distinctions, branch distinctions, and domain
          distinctions if a concrete workflow were supplied.
      Alpha:
        operator: "Α"
        applicability: "weak"
        refined_roles:
          - "Paper-specific fixed-point-oriented attractor schedules for controlled amplitude adjustment."
          - "Centre-aligned attractor regions used in spectral or phase-estimation-style settings."
        case_application: >
          Α is not refined through an explicit fixed-point attractor schedule or
          center-aligned spectral region. It remains weak because the Core and
          QC base readings already identify recurrence and possible attractor
          risk in iteration, but the case gives no amplitude-adjustment schedule
          or spectral/phase-estimation attractor structure.
      Theta:
        operator: "Θ"
        applicability: "weak"
        refined_roles:
          - "Paper-specific rendering of the controlled-unitary ladder U^(2^j) as a structured Θ–Ω pattern in QPE-related contexts."
          - "Optional iterative attractor-contraction schedules in fixed-point-style refinements."
        case_application: >
          Θ is central in the Core and QC base case because iteration depth is
          explicit, but the EXT-specific Θ refinement is only weak: no QPE
          ladder U^(2^j), no t value, and no attractor-contraction schedule are
          supplied. The extension therefore records a depth-governance concern
          without activating QPE ladder semantics.
      Phi:
        operator: "Φ"
        applicability: "weak"
        refined_roles:
          - "Paper-specific two-stage reframing from spectral / phase encoding toward computational readout."
        case_application: >
          Φ is active in the Core case as review recontextualization, but the
          EXT-specific spectral/phase-to-readout refinement is weak to not
          established. The case does not supply spectral encoding, phase
          representation, QFT alignment, or computational readout semantics.
      Psi:
        operator: "Ψ"
        applicability: "weak"
        refined_roles:
          - "Possible structural loci for invariance-bound commitments such as iteration ceilings or bounded coherence conditions."
          - "Paper-specific attractor-cycle validity constraints that aim to prevent runaway structural amplification without guaranteeing physical convergence."
        case_application: >
          Ψ appears weakly as a possible iteration ceiling, policy invariant,
          bounded coherence condition, or attractor-cycle validity constraint.
          It is not established as a workflow selfhood claim, machine commitment,
          physical stability guarantee, or convergence guarantee.

  macro_extensions:
    overview: >
      These macros are paper-specific refinements of the generic PMS-QC base
      layer. They are optional and intended for analysis, annotation,
      documentation, orchestration, and compilation-adjacent reasoning within
      the scope of the present paper. They do not claim completeness, physical
      sufficiency, optimal compilation, or validated control behaviour.
    selected_macro_extensions:
      - "MEASUREMENT_PROJECTION_MODEL"
    macros:
      QPE_ORACLE_LADDER:
        id: "QPE_ORACLE_LADDER"
        applicability: "not_applicable"
        refines_base_macro: "QORACLE_CALL"
        paper_dependency: "PMS-QC paper, QPE reconstruction / ladder refinement"
        params:
          - "unitary_id"
          - "t"
        description: >
          Paper-specific reusable refinement of the controlled power-of-two
          ladder U^(2^j) for j in [0, t-1] in QPE-related settings.
        pms_expansion:
          - "Θ"
          - "Ω"
          - "□"
        chain_pattern: "Θ^t( Ω ∘ □ )"
        notes: >
          This macro captures the paper-specific ladder refinement discussed in
          the QPE chapter. It does not alter the semantics of QORACLE_CALL in
          the generic base layer. The ladder itself remains EXT-only and does
          not imply optimality, necessity, correctness, or physical adequacy.
        case_application: >
          QPE_ORACLE_LADDER is not applicable because the case material does not
          specify QPE, a unitary_id, controlled powers U^(2^j), a ladder length
          t, eigenphase estimation, or QFT-related readout. The general concern
          with iteration depth is handled by PMS-QC base QITERATE rather than by
          this EXT ladder refinement.
      GROVER_CENTERING:
        id: "GROVER_CENTERING"
        applicability: "not_applicable"
        refines_base_macro: "QDIFFUSION"
        paper_dependency: "PMS-QC paper, Grover refinement / fixed-point discussion"
        params: []
        description: >
          Paper-specific refinement for fixed-point or overshoot-regulated
          Grover-style centering.
        pms_expansion:
          - "Α"
          - "Σ"
          - "Ψ"
        chain_pattern: "Ψ ∘ Σ ∘ Α"
        notes: >
          This refinement adds a Ψ-layer after attractor integration in order
          to model bounded, paper-specific regulation of Grover-style
          amplification. It is not part of standard Grover, is not required by
          the base representation, and does not guarantee convergence or
          overshoot prevention.
        case_application: >
          GROVER_CENTERING is not applicable because the case does not specify
          Grover search, a marked set, diffusion, amplitude amplification,
          centering, fixed-point Grover, or overshoot regulation. Ψ remains a
          weak governance invariant rather than Grover-specific regulation.
      ATTRACTOR_FIXED_POINT:
        id: "ATTRACTOR_FIXED_POINT"
        applicability: "weak"
        refines_base_macro: null
        paper_dependency: "PMS-QC paper, attractor schedule refinement discussion"
        params:
          - "schedule"
        description: >
          Paper-specific attractor schedule template for bounded fixed-point-
          oriented convergence modelling.
        pms_expansion:
          - "Α"
          - "Θ"
          - "Σ"
          - "Ψ"
        allowed_schedule_examples:
          - "linear"
          - "exponential"
          - "custom"
          - "under_specified"
        notes: >
          Supports optional schedule descriptions such as linear, exponential,
          or custom sequences. This is a structural refinement template only
          and does not guarantee actual convergence, correctness, stability, or
          physical adequacy.
        case_application: >
          ATTRACTOR_FIXED_POINT is weakly relevant because the case concerns
          repeated iteration, repetition limits, and possible stabilization, but
          no attractor schedule, convergence model, fixed-point condition,
          checkpoint cadence, or physical amplitude dynamics are supplied. It
          may be recorded as an under-specified future refinement, not as an
          active algorithmic macro. The weak status is important: the case gives
          enough material to ask whether repeated iteration should be bounded by
          checkpoints or invariants, but not enough material to name an
          attractor schedule, claim fixed-point behaviour, or treat the workflow
          as convergence-governed.
      MEASUREMENT_PROJECTION_MODEL:
        id: "MEASUREMENT_PROJECTION_MODEL"
        applicability: "weak"
        refines_base_macro: "QMEASURE"
        paper_dependency: "PMS-QC paper, measurement branching discussion"
        params: []
        description: >
          Paper-specific structural modelling refinement for branching in
          measurement domains.
        pms_expansion:
          - "Δ"
          - "Λ"
          - "□"
        notes: >
          Λ encodes unrealised branches, Δ actual outcome distinctions, and □
          the relevant measurement domain. This is a structural modelling aid,
          not a complete physical theory of measurement, and does not replace
          the base semantics of QMEASURE.
        case_application: >
          MEASUREMENT_PROJECTION_MODEL is weakly relevant because audit traces
          and review records can be treated as measurement-adjacent structural
          records: they distinguish actual trace outcomes from unrealised or
          non-selected alternatives within a declared audit frame. This does
          not imply a physical measurement operation, quantum collapse theory,
          basis specification, register specification, or trace validity. The
          model is useful only as an audit-branching discipline: it asks what
          was selected, what remained unrealised, what was expected but absent,
          and which frame made those distinctions meaningful.

  chain_patterns:
    overview: >
      These patterns derive directly from the PMS-QC paper and provide
      paper-specific chain templates for analysis, annotation, and comparison.
      They are non-canonical and do not override the semantics of the generic
      PMS-QC base overlay.
    selected_chain_patterns:
      - "none"
    patterns:
      qpe_exact:
        applicability: "not_applicable"
        description: >
          Paper-specific full QPE chain template with explicit ladder
          refinement.
        chain:
          - "Δ"
          - "∇"
          - "Α"
          - "Θ^t(Ω ∘ □)"
          - "Φ"
          - "Α"
          - "Σ"
          - "Δ"
          - "Λ"
        notes: >
          Matches QPE preparation, controlled ladder refinement, Fourier-domain
          reframing, alignment, and final measurement. The QFT-alignment stage
          is semantically ordered as Σ(Α(Φ(S))). The ladder component remains
          paper-specific and EXT-only.
        case_application: >
          The qpe_exact chain does not apply because the case does not specify
          QPE preparation, controlled power-of-two ladder, Fourier-domain
          reframing, QFT alignment, eigenphase readout, or measurement register.
          The EXT-only ladder discipline is preserved by not importing this
          pattern into the generic iteration-depth analysis.
      grover_fixed_point:
        applicability: "not_applicable"
        description: >
          Paper-specific Grover template with EXT-bound fixed-point-style
          regulation.
        chain:
          - "□"
          - "Δ"
          - "∇"
          - "Α"
          - "Θ^k(Ψ ∘ Σ ∘ Α ∘ Ω ∘ Δ ∘ □)"
          - "Δ"
          - "Λ"
        notes: >
          Standard Grover remains a base-layer construction without Ψ. This
          template adds EXT-bound Ψ regulation within the iterated block to
          model fixed-point or overshoot-regulated variants.
        case_application: >
          The grover_fixed_point chain does not apply because the case does not
          specify Grover search, diffusion, marked outcomes, or overshoot-
          regulated amplitude amplification. Ψ is kept as a weak governance
          invariant and is not treated as a base-Grover requirement.
      attractor_alignment:
        applicability: "not_applicable"
        description: >
          Paper-specific reframing-plus-alignment pattern for spectral or
          Fourier-oriented estimation settings.
        chain:
          - "Δ"
          - "Φ"
          - "Α"
          - "Σ"
        notes: >
          Useful where reframing and alignment are structurally foregrounded.
          This pattern is non-canonical and should not be conflated with the
          generic base macro semantics of QFT_ALIGN.
        case_application: >
          The attractor_alignment pattern does not apply because the case does
          not supply a spectral, Fourier, or phase-estimation setting. The Core
          case has Φ and possible Α, but not the EXT-specific spectral or
          Fourier-oriented alignment pattern.

  extended_policies:
    overview: >
      These are paper-specific structural guardrails and optional governance-,
      audit-, and stability-oriented refinements derived from the PMS-QC paper.
      They compose with, but never replace, PMS-QC base policies. They are
      non-guarantee refinements, not validated safety defaults.
    global_non_guarantee_clause: >
      Structural refinement ≠ physical guarantee ≠ safety guarantee.
    policy_status: "partial"
    selected_policies:
      - "basis_reframe_guard"
      - "attractor_cycle_integrity"
    policies:
      qpe_depth_guard:
        id: "qpe_depth_guard"
        applicability: "not_applicable"
        policy_kind: "governance"
        paper_specific: true
        non_guarantee: true
        description: >
          Bounded structural guard on QPE-style ladder depth. t_max expresses a
          paper-specific governance or audit bound, not a correctness or
          optimality guarantee.
        params:
          t_max: "not_applicable"
        rule: >
          Θ^t(Ω ∘ □) requires t less_than_or_equal_to t_max unless an explicit
          Ψ-stabilisation section follows.
        case_application: >
          QPE ladder depth is not applicable because the case does not provide
          QPE, a controlled-unitary ladder, t, or t_max. The general iteration
          problem remains handled by PMS-QC base max_iteration_depth rather than
          by the EXT qpe_depth_guard.
      grover_no_overshoot:
        id: "grover_no_overshoot"
        applicability: "not_applicable"
        policy_kind: "stability"
        paper_specific: true
        non_guarantee: true
        description: >
          Paper-specific overshoot-avoidance guard for Grover-style cycles with
          EXT-bound regulation.
        rule: >
          If Σ is applied after Α in Grover-style cycles, Ψ should follow within
          the same iteration scope unless alternative paper-specific invariance
          conditions are explicitly declared.
        case_application: >
          grover_no_overshoot is not applicable because the case does not
          specify Grover-style cycles or Α-after-Σ amplification within Grover
          iteration. The rule remains EXT-only and is not promoted into the
          generic governance case.
      basis_reframe_guard:
        id: "basis_reframe_guard"
        applicability: "active"
        policy_kind: "audit"
        paper_specific: true
        non_guarantee: true
        description: >
          Paper-specific guard protecting Σ-boundaries against unrestricted
          reframing.
        rule: >
          After Σ, Φ may reframe only if accompanied by explicit Ψ invariants.
        case_application: >
          basis_reframe_guard is active as an EXT audit guard because the case
          centrally concerns audit traces and review without self-validation. If
          any checkpoint, trace acceptance, review decision, or governance
          integration is treated as Σ-bound, later Φ must be explicitly logged
          as revision, review, or Ψ-bound update. This prevents silent post-hoc
          rationalization of workflow results.
      attractor_cycle_integrity:
        id: "attractor_cycle_integrity"
        applicability: "risk"
        policy_kind: "stability"
        paper_specific: true
        non_guarantee: true
        description: >
          Paper-specific attractor schedule guard for bounded structural
          admissibility.
        rule: >
          Α steps repeated under Θ should incorporate Σ checkpoints or Ψ
          invariants in order to avoid structurally unbounded divergence.
        case_application: >
          attractor_cycle_integrity is a risk-level policy because the case
          concerns repeated iteration and possible attractor-like stabilization,
          but no repeated Α steps, schedule, Σ checkpoints, Ψ invariants, or
          convergence conditions are supplied. The policy flags what would be
          needed before repeated attractor-like iteration could be treated as
          structurally bounded.

  ai_extension:
    status: "weak"
    overview: >
      This section defines optional reasoning affordances when PMS-QC_EXT is
      loaded. The capabilities below are advisory only and intended for bounded
      experimental settings. They do not imply deployment adequacy, validated
      safety, or full runtime autonomy.
    capabilities_added:
      - "May identify abstract ladder structures (Θ^t) and evaluate associated invariance rules."
      - "May suggest attractor schedules aligned with EXT-bound Ψ policies."
      - "May analyse reframing cascades (Φ → Φ → Φ) for structural consistency."
      - "May transform raw circuits into annotated Δ–Ψ chains using EXT macros and templates."
    guardrails:
      - "AI MUST treat PMS-QC_EXT as advisory and optional; Δ–Ψ fundamentals remain unchanged."
      - "AI MUST preserve the distinction between PMS base, PMS-QC base, and EXT refinements."
      - "AI MUST NOT interpret Ψ-invariants as physical stability guarantees."
      - "AI MUST NOT infer physical correctness, safety, or deployment readiness from EXT compliance."
      - "AI MUST NOT treat governance annotations as validated safety architecture."
      - "AI MUST NOT anthropomorphise quantum patterns."
    case_application: >
      AI-extension reasoning is weakly relevant for detecting under-specified
      depth, possible reframing cascades, missing invariance declarations, and
      attractor-cycle risk. It remains advisory and optional; it does not supply
      runtime autonomy, safety certification, physical correctness, or validated
      governance control.

  meta_notes:
    extensibility:
      - "Further QC-domain modules may be added, e.g. VQE_EXT, QML_EXT, TROTTER_EXT."
      - "All extensions MUST remain optional, Δ–Ψ-compatible and subordinate to PMS and PMS-QC."
      - "Paper-specific refinements MUST NOT be silently promoted into the generic base overlay."
      - "No extension may introduce new PMS meta-axioms."
    neutrality:
      - "Extensions refine interpretation but do not constrain foundational PMS."
      - "Extensions do not redefine PMS-QC base semantics."
      - "Extensions do not imply universality, comparative superiority, or final closure."
    versioning:
      - "Extensions evolve independently, but must declare compatible PMS and PMS-QC base versions."
      - "Semantic changes to a refinement should be versioned explicitly."
      - "Existing refinement semantics must not be silently overridden."
    case_application: >
      Extensibility, neutrality, and versioning issues are relevant only as
      guardrails. This case does not add VQE_EXT, QML_EXT, TROTTER_EXT, new
      semantics, or new PMS meta-axioms. Future QC-domain modules would require
      explicit extension status and versioning.

  qc_ext_case_output:
    output_type: "ext_policy_check"
    refinement_trace:
      status: "weak"
      elements:
        base_qc_overlay_dependency: "preserved"
        selected_ext_macros:
          - "MEASUREMENT_PROJECTION_MODEL"
        selected_chain_patterns:
          - "none"
        selected_ext_policies:
          - "basis_reframe_guard"
          - "attractor_cycle_integrity"
        non_guarantees:
          - "no physical correctness guarantee"
          - "no convergence guarantee"
          - "no stability guarantee"
          - "no safety guarantee"
          - "no deployment readiness claim"
      case_application: >
        The EXT refinement trace is weak and mostly negative: MEASUREMENT_PROJECTION_MODEL
        is weakly useful for audit-trace branching discipline, basis_reframe_guard
        is active for post-Σ audit reframing, and attractor_cycle_integrity is a
        risk-level check for repeated attractor-like iteration. No QPE or Grover
        chain is selected.
    ext_macro_annotation:
      status: "weak"
      macro_program: >
        MEASUREMENT_PROJECTION_MODEL(frame=audit_trace_under_specified,
        outcome_distinctions=under_specified, unrealised_branches=under_specified)
        with ATTRACTOR_FIXED_POINT(schedule=under_specified) recorded only as a
        non-active future refinement possibility. QPE_ORACLE_LADDER and
        GROVER_CENTERING are not applicable.
    ext_policy_check:
      status: "partial"
      checks:
        qpe_depth_guard: "not_applicable"
        grover_no_overshoot: "not_applicable"
        basis_reframe_guard: "satisfied"
        attractor_cycle_integrity: "weak"
      case_application: >
        The EXT policy-check outcome is bounded: basis_reframe_guard is
        satisfied as a stated guardrail because the overlay requires explicit
        Ψ-bound revision after any Σ checkpoint; attractor_cycle_integrity is
        weak because the case lacks schedule, checkpoint, and invariant details;
        qpe_depth_guard and grover_no_overshoot are not applicable.
    ext_chain_pattern_reading:
      status: "not_applicable"
      selected_pattern: "none"
      chain_summary: >
        No EXT chain pattern is warranted. qpe_exact, grover_fixed_point, and
        attractor_alignment require algorithmic structures not supplied by the
        case material. The Core and PMS-QC base signatures remain the operative
        structural representations.
    ai_extension_advisory:
      status: "weak"
      advisory_use: >
        AI–QC EXT advisory use may flag missing depth bounds, missing invariant
        declarations, possible post-Σ reframing, and absence of a concrete
        measurement/audit frame. It must not infer physical correctness, safety,
        deployment readiness, validated agent control, or implementation
        adequacy from those flags.

  qc_ext_overlay_quality_control:
    status: "pass_with_minor_notes"
    checks:
      pms_base_preserved: "true"
      qc_base_overlay_preserved: "true"
      core_case_preserved: "true"
      optional_extension_status_preserved: "true"
      no_new_pms_meta_axioms: "true"
      no_new_pms_operators: "true"
      no_dependency_redefinition: "true"
      no_qc_base_semantics_override: "true"
      ext_refinements_not_silently_promoted_to_base: "true"
      macros_marked_as_paper_specific: "true"
      chain_patterns_marked_as_non_canonical: "true"
      policies_marked_as_non_guarantee: "true"
      semantic_anchor_marked_non_canonical: "true"
      no_physical_correctness_guarantee: "true"
      no_convergence_guarantee: "true"
      no_stability_guarantee: "true"
      no_safety_guarantee: "true"
      no_deployment_readiness_claim: "true"
      no_validated_agent_control_claim: "true"
      no_architecture_independent_implementation_claim: "true"
      no_anthropomorphic_quantum_interpretation: "true"
      governance_conditionality_preserved: "true"
      base_vs_extension_boundary_checked: "true"
      ai_extension_guardrails_checked: "true"
      rival_formalisms_preserved: "true"
      non_capture_preserved: "true"
    issues_found:
      - "Minor note: EXT is only weakly triggered because the case is generic workflow governance, not QPE, Grover, spectral estimation, or a supplied attractor-schedule case."
      - "Minor note: MEASUREMENT_PROJECTION_MODEL is useful only as audit-trace branching discipline and must not be read as physical measurement theory."
      - "Minor note: attractor_cycle_integrity remains risk-level because no repeated Α schedule, Σ checkpoint cadence, or Ψ invariant is supplied."
      - "Minor note: the EXT overlay may appear textually rich despite weak applicability. This richness is documentary and boundary-forming, not evidence that the case activates the paper-specific algorithmic refinements."
    correction_required: "false"
    final_overlay_judgment: >
      The QC-EXT overlay is valid, bounded, source-conform, optional,
      non-guaranteeing, and correctly subordinated to PMS Core and PMS-QC base;
      it adds weak paper-specific audit and policy visibility without revising
      operators, dependencies, base QC semantics, or the completed Core pass.
      Its pass status means extension-layer conformity, not successful QPE,
      Grover, attractor-schedule, physical-measurement, implementation,
      governance, safety, or deployment validation.

  qc_ext_overlay_synthesis:
    primary_qc_ext_reading: >
      PMS-QC_EXT mainly makes visible that this case should not be over-read as
      a paper-specific QPE or Grover refinement case. Its strongest contribution
      is negative and boundary-forming: ladder, fixed-point Grover, and
      spectral-alignment templates are not warranted, while measurement-domain
      audit modelling and post-Σ reframing control are weakly useful.
    extension_trigger_result: >
      EXT is triggered only weakly and optionally. The trigger is not an
      algorithm-specific structure but the governance concern with iteration
      depth, audit trace, and review. This justifies an EXT policy check, not an
      active EXT algorithmic macro pass.
    base_vs_extension_result: >
      The distinction between PMS-QC base and QC-EXT refinements is preserved:
      generic QFRAME, QITERATE, QMEASURE, and QSTABILIZE remain base-overlay
      structures, while QPE_ORACLE_LADDER, GROVER_CENTERING,
      ATTRACTOR_FIXED_POINT, MEASUREMENT_PROJECTION_MODEL, and all EXT policies
      remain optional paper-specific refinements.
    extended_mapping_result: >
      Refined Δ, Α, Θ, Φ, and Ψ mappings are weak rather than active as
      algorithm-specific structures. Θ remains central only at the Core and
      QC-base level; no QPE ladder or fixed-point contraction schedule is
      supplied. Ψ remains policy-invariant shorthand, not physical stability or
      machine self-binding.
    macro_refinement_result: >
      MEASUREMENT_PROJECTION_MODEL is weakly applicable as audit-trace branching
      discipline. ATTRACTOR_FIXED_POINT is weak as an under-specified future
      schedule possibility. QPE_ORACLE_LADDER and GROVER_CENTERING are not
      applicable.
    chain_pattern_result: >
      No EXT chain pattern applies. qpe_exact, grover_fixed_point, and
      attractor_alignment remain non-canonical paper-specific templates that are
      not warranted by the supplied case material.
    policy_refinement_result: >
      basis_reframe_guard is active as a non-guaranteeing audit guard against
      unrestricted post-Σ reframing. attractor_cycle_integrity is risk-level
      because repeated iteration may require Σ checkpoints or Ψ invariants.
      qpe_depth_guard and grover_no_overshoot are not applicable.
    ai_extension_result: >
      AI-extension advisory use is weakly relevant for identifying missing
      invariants, missing depth bounds, under-specified measurement/audit frames,
      and possible reframing cascades. Deployment, safety, physical correctness,
      implementation adequacy, and validated agent-control overclaims are
      blocked.
    semantic_anchor_result: >
      DAG-style workflow modelling may be used only as non-canonical audit
      organization for iteration, trace, review, and policy-check nodes. It does
      not replace PMS operator logic, PMS-QC macro semantics, quantum circuit
      semantics, or implementation-specific workflow models.
    non_guarantee_result: >
      EXT structural refinement does not imply physical correctness,
      convergence, stability, safety, optimal compilation, implementation
      adequacy, governance validity, legal sufficiency, or deployment readiness.
    non_capture_result: >
      Outside PMS-QC_EXT capture remain quantum-mechanical correctness, circuit
      semantics, hardware noise, compilation constraints, algorithmic
      complexity, sampling validity, formal verification, empirical
      benchmarking, legal admissibility, institutional authority, and rival QC
      or governance formalisms.
    final_qc_ext_formula_or_signature: >
      MEASUREMENT_PROJECTION_MODEL(frame=audit_trace_under_specified) with
      basis_reframe_guard(post_sigma_phi_requires_explicit_psi_invariant) and
      attractor_cycle_integrity(status=risk, schedule=under_specified,
      checkpoints=under_specified, invariants=under_specified); no
      QPE_ORACLE_LADDER, no GROVER_CENTERING, and no selected EXT chain pattern.
    final_claim_boundary: >
      This QC-EXT overlay adds paper-specific visibility for QPE ladder
      refinements, fixed-point / overshoot-regulated Grover refinements,
      attractor schedule templates, measurement-domain modelling refinements,
      optional structural policy refinements, and advisory AI–QC reasoning. It
      does not revise PMS Base, rewrite the PMS-QC base overlay, introduce new
      PMS operators, redefine dependencies, replace QC formalisms, claim
      physical correctness, guarantee convergence, stability, safety, optimal
      compilation, deployment readiness, or validated agent control, and it
      does not claim final capture of quantum computation.